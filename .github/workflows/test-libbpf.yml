name: test-libbpf
on:
  workflow_dispatch:
jobs:
  call-reusable-workflow:
    uses: bobbintb/Slackware_Packages/.github/workflows/workflow-template.yml@main
    with:
      VERSION: "1.5.0"
      NAME: "libbpf"
