name: Build valkey
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  make_slackware_package:
    uses: bobbintb/Slackware_Packages/.github/workflows/sbopkg_template.yml@main
    secrets: inherit
    with:
      NAME: "valkey"
      PACKAGES: "make guile gc gcc kernel-headers binutils pkgconf"
